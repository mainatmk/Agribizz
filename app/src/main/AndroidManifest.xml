<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.agribizz">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AgriBizz">
        <activity
            android:name=".BuyerListActivity"
            android:exported="true" />
        <activity
            android:name=".BuyerDashboardActivity"
            android:exported="true" />
        <activity
            android:name=".DashboardActivity"
            android:exported="true" />
        <activity
            android:name=".SupplierDashboardActivity"
            android:exported="true" />
        <activity
            android:name=".SupplierList"
            android:exported="true" />
        <activity android:name=".BuyerActivity" />
        <activity android:name=".SupplierActivity" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <activity
            android:name=".SupplierMapsActivity"
            android:label="@string/title_activity_supplier_maps" />
        <activity android:name=".Shared" />
        <activity android:name=".RegisterList" />
        <activity android:name=".RegisterActivity">

        </activity>
        <activity android:name=".ListUsersActivity" />
        <activity android:name=".DisplayActivity" />
        <activity android:name=".ExampleActivity" />
        <activity
            android:name=".DrawerActivity"
            android:label="@string/title_activity_drawer"
            android:theme="@style/Theme.AgriBizz.NoActionBar">
            >
        </activity>
        <activity android:name=".SupplierProductActivity" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".MainActivity" >
        <intent-filter>
            <action android:name="android.intent.action.MAIN" />

            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter>
        </activity>

        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />
    </application>

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

</manifest>